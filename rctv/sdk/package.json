{
  "name": "rctv-sdk",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "watch": "bun build index.ts --outfile ../../static/app-sdk.js --watch",
    "bundle": "bun build index.ts --outfile ../../static/app-sdk.js --target=browser --minify"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
